# Core inference (all you need to run DANTE):
torch>=2.0
numpy>=1.21

# Optional extras:
pandas>=1.3        # live driver fetching (dante.drivers / examples/02_live_nowcast.py)
matplotlib>=3.6    # plotting in the CLI and examples

# Or install everything as a package:  pip install ".[drivers,plot]"
